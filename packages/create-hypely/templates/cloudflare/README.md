# Cloudflare Workers + Hypely

This is a simple Cloudflare Workers application using the Hypely library.

## Installation

To install dependencies, run:

```bash
npm install
```

## Usage

To start the application, run:

```bash
npm dev
```